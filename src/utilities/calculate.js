const add = (first, second) => {
    return first + second;
}

const multiply = (first, second) => {
    return first * second;
}

//2n option

export { add, multiply };


//1st option

// export default add--;